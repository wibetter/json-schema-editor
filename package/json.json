{
  "type": "object",
  "title": "empty object",
  "properties": {
    "func": {
      "type": "object",
      "format": "func",
      "description": "功能设置",
      "readOnly": true,
      "properties": {
        "setColor": {
          "type": "string",
          "description": "设置颜色"
        },
        "datetime": {
          "type": "string",
          "format": "date-time",
          "description": "Date-time"
        },
        "time": {
          "type": "string",
          "format": "time",
          "description": "Time"
        }
      },
      "required": ["setColor", "field_10", "field_11"]
    },
    "style": {
      "type": "object",
      "format": "style",
      "description": "样式设置",
      "readOnly": true,
      "properties": {
        "colorItem": {
          "type": "string",
          "format": "color",
          "description": "颜色"
        },
        "radioItem": {
          "type": "string",
          "format": "radio",
          "enum": ["a", "b"],
          "enumextra": ["选项a", "选项b"],
          "description": "单选"
        },
        "selectItem": {
          "type": "array",
          "format": "select",
          "items": {
            "type": "string",
            "enum": ["a", "b", "c"],
            "enumextra": ["选项a", "选项b", "选项c"]
          },
          "uniqueItems": true,
          "description": "多选"
        },
        "urlItem": {
          "type": "string",
          "format": "url",
          "description": "Url"
        }
      },
      "required": ["colorItem", "radioItem", "selectItem", "urlItem"]
    },
    "data": {
      "type": "object",
      "format": "data",
      "description": "数据设置",
      "readOnly": true,
      "properties": {
        "aa": {
          "type": "object",
          "format": "datasource",
          "properties": {
            "name": {
              "type": "string",
              "faker": "lorem.word",
              "description": "名字"
            },
            "filter": {
              "type": "string",
              "format": "textarea",
              "default": "return data;",
              "description": "过滤器"
            },
            "type": {
              "type": "string",
              "default": "local",
              "format": "typeSelect",
              "enum": ["local", "remote"],
              "enumextra": ["local", "remote"],
              "description": "类型"
            }
          },
          "description": "数据源",
          "required": ["name", "filter", "type"]
        },
        "quantity": {
          "type": "object",
          "format": "quantity",
          "properties": {
            "unit": {
              "type": "number",
              "description": "数量"
            },
            "quantity": {
              "type": "string",
              "default": "em",
              "format": "quantitySelect",
              "enum": ["px", "rem", "em", "percent"],
              "enumextra": ["px", "rem", "em", "percent"],
              "description": "单位"
            }
          },
          "description": "单位计量输入",
          "required": ["unit", "quantity"]
        },
        "eventItem": {
          "type": "object",
          "format": "event",
          "properties": {
            "name": {
              "type": "object",
              "format": "datasource",
              "properties": {
                "name": {
                  "type": "string",
                  "faker": "lorem.word",
                  "description": "名字"
                },
                "filter": {
                  "type": "string",
                  "format": "textarea",
                  "default": "return data;",
                  "description": "过滤器",
                  "readOnly": true
                },
                "type": {
                  "type": "string",
                  "default": "local",
                  "format": "typeSelect",
                  "enum": ["local", "remote"],
                  "enumextra": ["local", "remote"],
                  "description": "类型"
                }
              },
              "description": "数据源",
              "required": ["name", "filter", "type"]
            },
            "filter": {
              "type": "string",
              "format": "textarea",
              "default": "return data;",
              "description": "过滤器",
              "readOnly": true
            },
            "type": {
              "type": "string",
              "default": "out",
              "format": "typeSelect",
              "enum": ["in", "out"],
              "enumextra": ["in", "out"],
              "description": "类型"
            }
          },
          "description": "事件",
          "required": ["name", "filter", "type"]
        },
        "jsonItem": {
          "type": "string",
          "format": "json",
          "description": "JSON"
        }
      },
      "required": ["a a", "quantity", "eventItem", "jsonItem"]
    }
  },
  "required": ["func", "style", "data"]
}
